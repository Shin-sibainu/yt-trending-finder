/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // App Router on edge-friendly features; route files can set runtime='edge'.
  }
};

export default nextConfig;

